import 'package:flutter/material.dart';

class AddTasksViewBody extends StatelessWidget {
  const AddTasksViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
